# Perfil local (Docker Compose en desarrollo). Activa Swagger sobre prod.
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

logging:
  level:
    root: INFO
    com.supermarket: DEBUG
